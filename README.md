## СОДЕРЖАНИЕ

1. Установка и подготовка среды
2. Исходный код
3. Получение AST (Abstract Syntax Tree)
4. Генерация LLVM IR
5. Оптимизация IR
6. Построение и анализ CFG (Control Flow Graph)
7. Выводы по основной части
8. Дополнительное задание: исследование оптимизаций LLVM

---

## 1. Установка и подготовка среды

**Цель этапа** — настроить систему так, чтобы были доступны все необходимые инструменты: компилятор Clang, утилиты LLVM для работы с IR, а также Graphviz для визуализации графов.

1.1. **Обновление списка пакетов**

```bash
sudo apt update
```

— Обновляет локальный кэш пакетов, чтобы последующие установки прошли по последним метаданным.

1.2. **Установка Clang, LLVM, Opt и Graphviz**

```bash
sudo apt install -y clang llvm opt graphviz
```

* `clang` — фронтенд для языков C/C++;
* `llvm` — базовый пакет инструментов LLVM (включает библиотеки и утилиты);
* `opt` — утилита для применения оптимизаций к LLVM IR;
* `graphviz` — инструмент для генерации PNG/SVG из DOT-файлов (CFG).

> **Почему эти пакеты?**
>
> * Clang генерирует AST и IR.
> * Opt позволяет экспериментировать с разными наборами оптимизаций.
> * Graphviz нужен для визуального анализа структуры потока управления, что критично для понимания того, как оптимизации меняют логику.

📸 *Скриншот 1*: вывод `apt install` — показывает, что все пакеты успешно установлены, без конфликтов и ошибок.

---

## 2. Исходный код

**Цель этапа** — подготовить минимальный, но достаточный пример C‑программы, демонстрирующий функцию и её вызов, чтобы увидеть, как оптимизации влияют на инлайнинг, SSA‑преобразование и удаление мёртвого кода.

```c
// файл: main.c
#include <stdio.h>

// Функция возведения в квадрат
int square(int x) {
    return x * x;
}

int main() {
    int a = 5;                  // инициализация переменной
    int b = square(a);          // вызов функции square
    printf("%d\n", b);          // вывод результата в консоль
    return 0;                   // успешный завершение
}
```

* **Почему именно такой пример?**

  * Небольшая функция (`square`) — идеально подходит для просмотра эффекта `-inline`.
  * Использование константы (`5`) позволяет Clang провести `constprop` (подстановка константы).
  * Простая программа, чтобы не отвлекаться на сложные конструкции.

📸 *Скриншот 2*: содержимое `main.c` в VS Code или через `cat main.c`.

---

## 3. Получение AST

**AST (Abstract Syntax Tree)** — древовидное представление исходного кода на уровне синтаксиса языка (C).

Команда:

```bash
clang -Xclang -ast-dump -fsyntax-only main.c
```

* `-Xclang -ast-dump` — передаёт флаг непосредственно фронтенду Clang для дампа AST;
* `-fsyntax-only` — указывает проверить синтаксис, но не генерировать объектный код.

Результат — текстовый вывод, где каждому узлу AST соответствует строка с указанием типа (`FunctionDecl`, `VarDecl`, `CallExpr` и т. д.).

**Ключевые моменты в AST**:

* `FunctionDecl` для `square` и `main` с указанием возвращаемого типа и параметров.
* `CompoundStmt` внутри каждой функции — блок операторов.
* `CallExpr` указывает вызов `printf` и `square`.
* Листовые узлы (`IntegerLiteral`) показывают литеральные значения `5`, `x`, форматную строку `"%d\n"`.

📸 *Скриншот 3*: пример начинающих 15–20 строк AST, чтобы было видно структуру.

---

## 4. Генерация LLVM IR

**LLVM IR** — полумашинный язык со статической типизацией и SSA‑формой, на который Clang транслирует C-код перед оптимизациями и генерацией машинного кода.

Команда:

```bash
clang -S -emit-llvm main.c -o main.ll
```

* `-S` — генерировать человекочитаемый текстовый файл, а не объектный код;
* `-emit-llvm` — указать формат IR;
* `-o main.ll` — имя выходного файла.

**Что мы видим в `main.ll`**:

* Функции `@square` и `@main` с префиксом `define`;
* Операции `alloca` создают на стеке память для локальных переменных;
* Инструкции `store` и `load` для записи и чтения значений из памяти;
* Явное обращение к внешнему символу `@printf`.

📸 *Скриншот 4*: первые 20 строк файла `main.ll`.

---

## 5. Оптимизация IR

### 5.1. IR без оптимизации

```bash
clang -O0 -S -emit-llvm main.c -o main_O0.ll
```

* `-O0` — уровень оптимизации «никаких оптимизаций».
* В `main_O0.ll` вы увидите **массив alloca**, **множество load/store** и **отдельную функцию** `@square`.

📸 *Скриншот 5*: первые 15 строк `main_O0.ll`.

### 5.2. IR с оптимизацией -O2

```bash
clang -O2 -S -emit-llvm main.c -o main_O2.ll
```

* `-O2` включает комплексный набор оптимизаций (около 30), среди которых:

  * `-inline` (встраивание функций);
  * `-constprop` (вычисление констант на этапе компиляции);
  * `-mem2reg` (продвижение всехока в регистры — SSA);
  * `-instcombine` (сведение и упрощение инструкций);
  * `-simplifycfg` (упрощение структуры блоков);
  * другие: `-reassociate`, `-gvn`, `-sroa`, `-dce` и др.

**Изменения, которые вы заметите**:

1. **Отсутствие функции `square`** — она встроена в `main` и затем устранена, поскольку вызов стал константным.
2. **Нет `alloca`, `load`, `store`** — все локальные переменные преобразованы в SSA-регистры.
3. **Один вызов `printf`** с уже известным значением `25` (5²).

📸 *Скриншот 6*: первые 15 строк `main_O2.ll`.
📸 *Скриншот 7*: вывод `diff main_O0.ll main_O2.ll` с ключевыми удалёнными и добавленными строками.

---

## 6. Построение и анализ CFG

**CFG** — ориентированный граф, где узлы — базовые блоки, а рёбра — возможные переходы управления.

### 6.1. Генерация .dot‑файла

```bash
opt -dot-cfg -disable-output main_O2.ll
```

* `-dot-cfg` — генерирует DOT‑файлы для каждой функции в IR;
* `-disable-output` — не выводить новый IR, только DOT‑файлы.

В результате получаем файл `.main.dot` (точка в начале — особенность LLVM).

### 6.2. Конвертация в PNG

```bash
dot -Tpng .main.dot -o cfg_main.png
xdg-open cfg_main.png
```

**Что будет видно**:

* Один узел (блок) с инструкциями `call printf`, `ret`;
* Поскольку `square` удалена, CFG крайне прост: единственный путь без разветвлений.

📸 *Скриншот 8*: первые 10 строк `.main.dot`.
📸 *Скриншот 9*: визуализация `cfg_main.png`.

---

## 7. ВЫВОДЫ ПО ОСНОВНОЙ ЧАСТИ

1. **Clang + LLVM** дают полный контроль над этапами компиляции: от AST до IR и машинного кода.
2. **AST** помогает анализу на уровне синтаксиса (например, для статического анализа, линтеров).
3. **LLVM IR** в SSA‑форме удобен для реализации собственных трансформаций и оптимизаций.
4. Оптимизация `-O2` показывает, как компилятор:

   * Встраивает функции и вычисляет выражения на этапе компиляции;
   * Устраняет лишние загрузки/сохранения памяти;
   * Преобразует код в более компактный и эффективный.
5. **CFG** после оптимизации упрощается, что облегчает дальнейший анализ и генерацию машинного кода.

---

## 8. ДОПОЛНИТЕЛЬНОЕ ЗАДАНИЕ: ИССЛЕДОВАНИЕ ДОПОЛНИТЕЛЬНЫХ ОПТИМИЗАЦИЙ

**Вариант 11:** выбраны оптимизации `-loop-unroll`, `-licm`, `-dse`.

### 8.1. Исходный код

```c
// файл: sum.c
#include <stdio.h>

int sum_array(int* arr, int n) {
    int sum = 0;
    for (int i = 0; i < n; ++i)
        sum += arr[i];
    return sum;
}

int main() {
    int data[5] = {1,2,3,4,5};
    int total = sum_array(data, 5);
    printf("Sum = %d\n", total);
    return 0;
}
```

📸 *Скриншот 10*: содержимое `sum.c`.

### 8.2. IR до оптимизации

```bash
clang -O0 -S -emit-llvm sum.c -o sum_O0.ll
```

📸 *Скриншот 11*: первые 15 строк `sum_O0.ll`.

### 8.3. Применение дополнительных оптимизаций

```bash
opt -S -loop-unroll -licm -dse sum_O0.ll -o sum_custom.ll
```

* `-loop-unroll` — разворачивание циклов (увеличивает тело цикла, уменьшает число итераций циклов на уровне IR);
* `-licm` (Loop Invariant Code Motion) — вынос инвариантных операций за пределы цикла;
* `-dse` (Dead Store Elimination) — удаление бессмысленных операций записи.

📸 *Скриншот 12*: первые 15 строк `sum_custom.ll`.

### 8.4. Сравнение IR

```bash
diff sum_O0.ll sum_custom.ll
```

📸 *Скриншот 13*: ключевые отличия:

* Удалены лишние `store` в теле цикла;
* Цикл частично развёрнут (если LLVM оценил, что `n=5` константа и допустимо полностью развернуть);
* Инвариантные вычисления (адрес `arr` + смещение) вынесены из тела цикла.

### 8.5. CFG до и после

```bash
opt -dot-cfg -disable-output sum_custom.ll
dot -Tpng .main.dot -o cfg_sum.png
xdg-open cfg_sum.png
```

📸 *Скриншот 14*: `cfg_sum.png` — видно, что блоков стало меньше, и они линейнее.

---

## ЗАКЛЮЧИТЕЛЬНЫЕ ВЫВОДЫ

1. **Loop Unroll** улучшает производительность за счёт снижения числа переходов, но увеличивает размер кода.
2. **LICM** выносит выражения, не зависящие от итерации, экономя повторные вычисления.
3. **DSE** убирает записи, не влияющие на внешний результат, очищая IR.
4. Совокупное применение специфичных оптимизаций позволяет тонко настраивать генерацию кода под конкретные нужды (встраивания, использование SIMD‑векторизации и пр.).
